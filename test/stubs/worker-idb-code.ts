// Test stub for the Vite virtual module `virtual:worker-idb-code`.
// The real value is the bundled worker IndexedDB shim injected at build time;
// under test we never spawn TDLib workers, so an empty string is enough.
export const WORKER_IDB_CODE = '';
